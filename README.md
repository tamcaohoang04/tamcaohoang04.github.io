# tamcaohoang04.github.io

**Do not move, rename or delete `index.html`, `privacy.html` or `terms.html`.**

These three pages are the home page, privacy policy and terms of service registered on a
**published Google OAuth app** (Google Cloud project `openclaw-calendar-506205`, the
"OpenClaw Calendar" consent screen). Google verifies these URLs; if they 404 the app can be
flagged and the OAuth tokens behind a running personal assistant stop being refreshable.

Source of truth for these files lives in the private `jet-ops` repo (`site/`). Edit there,
then copy here.
